# config.py
import os

# =========================================================
# [1] 데이터 경로 설정
# =========================================================

# 1. 오디오 파일들이 들어있는 최상위 폴더 경로
# (상대 경로 "../" 보다는 절대 경로 "C:/Users/..." 추천)
BASE_DIR = r"C:\Users\myhom\Jihoon\3학년 2학기\캡스톤 디자인\금감원 피싱 데이터\보이스 피싱 데이터(금감원)"

# 2. BASE_DIR 안에 있는 처리할 세부 폴더명 리스트
INPUT_FOLDERS = [
    "바로 이 목소리",
    "대출 사기형",
    "수사기관 사칭형",
    # "일반 대화"  # 일반 대화 폴더도 포함
]

# [수정] 결과물 저장 경로 (보이스피싱 전용 중간 산출물)
OUTPUT_DIR = "./logs_and_metadata"
CSV_PATH = "./dataset_voice_phishing.csv" # 변경: 보이스피싱 데이터만 저장됨

# =========================================================
# [2] 외부 데이터 병합 설정 (KorCCVi_v2 연동)
# =========================================================

# [신규] 일반 대화 데이터 경로 (KorCCVi_v2.csv)
NORMAL_CSV_PATH = "../KorCCVi_v2.csv" 

# [신규] 최종 병합된 마스터 데이터셋 경로
MASTER_CSV_PATH = "./dataset_master.csv"

# [신규] KorCCVi_v2 컬럼 매핑 규칙
# 외부 데이터의 컬럼명을 우리 데이터셋 표준(script, label)에 맞춤
NORMAL_COLUMN_MAPPING = {
    "text": "transcript",  # 우리가 필요한 텍스트가 있는 컬럼명
    "id": "id"             # 원본 ID 컬럼명
}

# =========================================================
# [3] 라벨링 및 클래스 규칙 (기존 유지)
# =========================================================
FOLDER_LABEL_MAP = {
    "일반 대화": 0,
    "바로 이 목소리": 1,
    "대출 사기형": 1,
    "수사기관 사칭형": 1
}

FOLDER_CLASS_MAP = {}

# =========================================================
# [4] 모델 및 시스템 설정 (기존 유지)
# =========================================================
WHISPER_MODEL_SIZE = "deepdml/faster-whisper-large-v3-turbo-ct2"
SEED = 42
DEVICE = "cuda"